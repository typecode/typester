const Features = function (opts) {
    const { animation } = opts;

    const features = document.querySelector('section.features');

    // animation.animate(features, () => { console.log('features animate!'); });
};

export default Features;
